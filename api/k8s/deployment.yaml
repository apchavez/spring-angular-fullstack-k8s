# NOT deployed via `kubectl apply`. The real manifest lives in chart/templates/deployment.yaml
# (Helm) and is what deploy.yml actually applies (`helm upgrade --install`).
#
# This file only exists so CI has somewhere to persist the last-built image tag:
# ci.yml's docker-api job sed-patches the line below after each build, and deploy.yml
# greps it to read the tag it passes to `helm upgrade --set api.image.tag=...`.
image: ghcr.io/apchavez/spring-angular-fullstack-k8s-api:sha-840a388
